# .github/workflows/bot.yml
name: Deploy to Render

on:
  push:
    branches:
      - main  # or the branch you're using

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Render to Deploy
        run: |
          curl -X POST https://api.render.com/deploy/srv-cvoatm3uibrs73bn68o0?key=dep-cvob686mcj7s7380sa7g

